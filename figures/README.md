# figures/

The ten paper figures live in `figures/paper/` and are produced by `src/figures_paper.py`. Every plotted number comes from the model or from `results/` (the same sources as `results/numbers.json`); nothing is drawn by hand. Style (module header): Liberation Sans 8 pt body and 7 pt ticks, Okabe-Ito colour-blind-safe palette with sites running warm to cool from south to north, no in-figure titles, top and right spines removed, 600 dpi PNG with a 0.05 in `savefig` pad, column width 3.50 in and page width 7.16 in (`COL_W`, `PAGE_W`).

## Figures

| File | Function | Content |
|---|---|---|
| `paper/fig01_monthly_ghi.png` | `fig_ghi` | Monthly global horizontal irradiation (kWh/m²) at the four sites from the PVGIS-SARAH2 typical meteorological year; legend gives the annual GHI (Southampton 1,133, Birmingham 1,028, Liverpool 1,018, Edinburgh 945 kWh/m²). |
| `paper/fig02_architecture.png` | `fig_architecture` | Block schematics: (a) solar PV array (500 to 850 Wp, latitude tilt, south), MPPT charge controller (efficiency about 97%), LFP battery (1.5 to 2.0 kWh, 80% DoD) and the 14.64 W DC load, with 2 site visits per year and no fuel or oil; (b) diesel A2: 2 kVA-class set (1.6 kW, 0.9 h/day at 30% load) feeding rectifier, charge and auto-start controllers, lead-acid buffer (about 2.4 kWh, 4-year life), 200 L fuel tank (about 84 L/yr used) and the same DC load, with 12 site visits per year. The hardware labels are fixed text in `fig_architecture()` and currently match the final designs; the authoritative values are in `docs/assumptions.md` and `results/numbers.json`. |
| `paper/fig03_validation.png` | `fig_validation` | Four panels (one per site): monthly specific yield without system losses. Grey band and black line: PVGIS 2005-2020 range and sixteen-year mean; dashed line: this study's chain driven by the same PVGIS plane-of-array inputs (validation layer 1); markers: this study's TMY chain (layer 2). Each panel is annotated with the same-input annual bias and the TMY bias. Reads `results/validation.json`. |
| `paper/fig04_sizing_landscape.png` | `fig_sizing_landscape` | Four panels: governing-year LOLP (log colour scale, 0.01% to 100%) over the step-1 grid of PV rating x battery capacity from `results/sizing_sweep_<site>.csv`, white contour at LOLP 1%, open circle at the step-1 TMY design and star at the final design. |
| `paper/fig05_multiyear.png` | `fig_multiyear` | 2 x 2 grid of 4 x 16 heat maps of annual LOLP (%) for 2005-2020 under governing-year conditions: step-1 TMY designs under (a) this study's chain and (b) the PVGIS chain; final designs under (c) this study's chain and (d) the PVGIS chain. Cells above 1% are outlined and printed in bold; the colour scale saturates at 3%. Reads `results/multiyear.json`. |
| `paper/fig06_worst_event.png` | `fig_worst_event` | The worst sixteen-year event for the Edinburgh final design (850 Wp + 1.75 kWh) under governing-year conditions, this study's chain: twelve days ending 11 December 2010 (the ten-day window with the most unmet hours). (a) PV output and load (W), (b) battery state of charge with the 20% DoD floor, (c) hourly unmet load (Wh). |
| `paper/fig07_lcoe_breakdown.png` | `fig_lcoe_and_breakdown` | (a) LCOE by site: solar-battery bars at 5% and 8% (values printed), diesel A2 as reference lines at 5% (£22.72/kWh) and 8% (£23.52/kWh), diesel A1 at 5% noted off scale (£75.2/kWh); (b) discounted 25-year cost at 5% for the Southampton solar system (£11.8k) and diesel A2 (£41.1k), split into capital, replacements, O&M / oil, fuel and site visits. |
| `paper/fig08_tornado.png` | `fig_tornado` | One-at-a-time sensitivity of the diesel A2 to solar LCOE ratio (Southampton final design, 5%), ten parameters: solar visits 1 to 6/yr, diesel visits 12 to 2/yr, visit cost x 0.5 to x 2, discount rate 3 to 10%, PV capital +/-30%, load +/-20% with re-sized designs, battery capital +/-30%, LFP life 8 to 15 yr, degradation 0.3 to 1.0%/yr with re-sized designs, diesel price 45 to 118 p/L. Dashed line at the central ratio 3.49, dotted line at parity. Values from `sensitivity.tornado_data`. |
| `paper/fig09_monte_carlo.png` | `fig_monte_carlo` | (a) Probability densities of the solar LCOE per site and of the diesel A2 LCOE at 5% over 5,000 joint draws (the distributions do not overlap); (b) P10 to P90 bars and P50 markers of the diesel-to-solar ratio per site at 5% (circles) and 8% (squares) with the parity line. Runs `monte_carlo.run_monte_carlo` in-process (seed 42). |
| `paper/fig10_sweep2d.png` | `fig_sweep2d` | Four panels: diesel A2 to solar LCOE ratio over battery capital cost (£350 to £1,400/kWh) x discount rate (3 to 10%), contours from 2.25 to 4.0, star at the central point (£700/kWh, 5%). Values from `sweeps_2d.ratio_matrix`. |

## To regenerate

```bash
python -m src.figures_paper
```

Requires `results/validation.json`, `results/multiyear.json` and `results/sizing_sweep_<site>.csv` (written by `python -m src.validation`, `python -m src.multiyear` and `python -m src.make_results`) and the committed weather files in `data/pvgis/` and `data/pvgis_series/`; the figures are set in Liberation Sans with Liberation Serif panel labels. `python -m src.make_all` runs the whole pipeline and ends with the figures and the tests. Legacy figure modules and the root-level `figures/*.png` from earlier versions were removed; the paper figures are the only figure outputs.
